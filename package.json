{
  "name": "welkom-substitution-sms-tool",
  "version": "1.0.0",
  "description": "Welkom USA internal substitution SMS tool for Netlify.",
  "scripts": {
    "test": "node --test",
    "dev": "node scripts/dev-server.js",
    "build": "node scripts/build-check.js",
    "lint": "node --check netlify/functions/api.js && node --check src/auth.js && node --check src/data-store.js && node --check src/history.js && node --check src/shopify.js && node --check src/sms.js && node --check scripts/dev-server.js",
    "format": "node scripts/build-check.js",
    "typecheck": "node --check src/data-store.js",
    "credential-scan": "node scripts/credential-scan.js"
  },
  "dependencies": {
    "@netlify/blobs": "^10.1.0",
    "dotenv": "^16.4.7",
    "twilio": "^5.4.3"
  },
  "engines": {
    "node": ">=18"
  },
  "private": true,
  "license": "UNLICENSED"
}
